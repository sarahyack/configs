# GTK pack inventories

These files intentionally store only the top-level names of GTK themes, icon packs, and cursor packs.

The backup script regenerates these lists from the current system. The restore script reads them and reports where they are, but it does not copy or install the pack assets. Install the packs independently on a new system, then run the restore script for the regular config files.
